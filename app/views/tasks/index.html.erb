<div class="container">
  <h1>My Tasks</h1>
  <% @tasks.each do |task| %>
    <h6><%= link_to "#{task.title}", task_path(task.id) %></h6>
  <% end %>

  <div class="btn btn-success"><%= link_to "Create a new task", new_task_path %></div>
</div>
