<div class="container">
  <h1>My Tasks</h1>
  <% @tasks.each do |task| %>
    <h4><%= link_to "#{task.title}", task_path(task.id) %></h4>
  <% end %>

  <%= link_to "Create a new task", new_task_path %>
</div>
