<div class="container">
  <h1><%= @task.title %></h1>

  <h4>Details: <%= @task.details %></h4>

  <%= link_to "home", tasks_path %>
  <%= link_to "edit", edit_task_path %>
  <%= link_to "delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete #{@task.title}?"} %>
</div>
